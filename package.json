{
  "name": "sport-tables",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "watch": "npx vite build --watch"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.87.0",
    "vite": "^6.3.1"
  }
}
